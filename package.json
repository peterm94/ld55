{
  "name": "lagom-game-template",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && butler push dist quackqack/ld55:html5"
  },
  "dependencies": {
    "lagom-engine": "^1.1.0",
    "webfontloader": "^1.6.28"
  },
  "devDependencies": {
    "@types/webfontloader": "^1.6.34",
    "typescript": "^5.0.2",
    "vite": "^5.2.3"
  }
}
